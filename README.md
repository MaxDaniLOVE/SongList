Before start project run next scripts:

- `npm i` to install dependencies;
- `npm run server` start server with data at <http://localhost:3000/>
- `npm start` to run project (http://localhost:8080/);
